export type Product = {
    title: string,
    price: number,
    description: string,
    sizes: string[]
    
}

export type CartItem = {
    title: string,
    quantity: number,
    price: number,
    size: string
  }